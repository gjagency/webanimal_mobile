import 'package:flutter/material.dart';

class PageAuthOTP extends StatelessWidget {
  const PageAuthOTP({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
