import 'package:flutter/material.dart';

class PageProfilesPublic extends StatelessWidget {
  const PageProfilesPublic({super.key, required String username});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
