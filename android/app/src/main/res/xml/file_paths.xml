<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permite compartir archivos desde cache -->
    <cache-path
        name="cache"
        path="." />

    <!-- Permite compartir archivos desde files -->
    <files-path
        name="files"
        path="." />

    <!-- Permite compartir imágenes (image_picker, camera, etc) -->
    <external-cache-path
        name="external_cache"
        path="." />

    <external-files-path
        name="external_files"
        path="." />

</paths>
